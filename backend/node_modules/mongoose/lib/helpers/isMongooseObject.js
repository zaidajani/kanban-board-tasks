'use strict';

const isMongooseArray = require('../types/array/isMongooseArray').isMongooseArray;


module.exports = function(v) {
  return (
    v != null && (
      isMongooseArray(v) || 
      v.$__ != null || 
      v.isMongooseBuffer || 
      v.$isMongooseMap 
    )
  );
};
